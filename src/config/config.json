{
  "development": {
    "username": "${DB_USER}",
    "password": "${DB_PASS}",
    "database": "${DB_NAME}",
    "host": "${DB_HOST}",
    "dialect": "${DB_DRIVER}",
    "port": "${DB_PORT}"
  },
  "test": {
    "username": "${DB_USER}",
    "password": "${DB_PASS}",
    "database": "${DB_NAME}_test",
    "host": "${DB_HOST}",
    "dialect": "${DB_DRIVER}",
    "port": "${DB_PORT}"
  },
  "production": {
    "username": "${DB_USER}",
    "password": "${DB_PASS}",
    "database": "${DB_NAME}_production",
    "host": "${DB_HOST}",
    "dialect": "${DB_DRIVER}",
    "port": "${DB_PORT}"
  }
}